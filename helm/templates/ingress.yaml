apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-rules
spec:
  rules:
  - host: nginx-vprofileproject.com
    http:
      paths:
      - path: /
        pathType: Exact
        backend:
          service:
            name: vproapp-service
            port:
              number: 80