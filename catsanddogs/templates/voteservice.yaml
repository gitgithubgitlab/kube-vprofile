# apiVersion: v1
# kind: Service
# metadata:
#   name: vproapp-service
# spec:
#   selector:
#     app: vproapp
#   ports:
#     - port: 80
#       targetPort: vproapp-port
#       protocol: TCP
#   type: NodePort

apiVersion: v1
kind: Service
metadata:
  name: vote
spec:
  selector:
      app: vote
  ports:
    - port: 5000
      targetPort: vote-app-port  
      protocol: TCP
  type: LoadBalancer


